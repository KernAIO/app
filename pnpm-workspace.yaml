# Umbrella DEV workspace only. Each repo under repos/ is its own git repository
# (see scripts/dev-setup.sh). pnpm links all @kernhq/* packages locally here.
packages:
  - 'repos/*'
  - 'repos/*/packages/*'
